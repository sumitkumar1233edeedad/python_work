""" 

"""

import numpy as np

# Create a NumPy array
arr = np.array([10, 20, 30, 40])

# Append elements [20, 30, 40] to the array
new_arr = np.append(arr, [20, 30, 40])

# Print the new array
print(new_arr)